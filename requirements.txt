# --- Web Framework & Server ---
flask
flask-cors
gunicorn

# --- Asynchronous Task Queue ---
celery
redis

# --- Core ML & Image Libraries ---
# By not specifying versions for torch, torchvision, and torchaudio,
# pip will install versions compatible with the base image if they aren't already present.
torch
torchvision
torchaudio
Pillow
numpy
# This package is specific to the CUDA version in the base image.
cupy-cuda12x

# --- Hugging Face Libraries ---
# diffusers is pinned to a specific commit for stability.
diffusers @ git+https://github.com/huggingface/diffusers.git
dfloat11
transformers
huggingface-hub
safetensors
accelerate

# --- Utilities & Logging ---
python-dotenv
loguru
requests
pyyaml